<UserControl x:Class="SportBet.AnalyticControls.UserControls.SportRatingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:ViewModel="clr-namespace:SportBet.DesignTimeViewModels"
             d:DesignHeight="400" d:DesignWidth="500"
             Background="{StaticResource DefaultBackgroundColor}"
             d:DataContext="{d:DesignInstance ViewModel:SportRatingDTViewModel}">
    <GroupBox Header="Sport Ratings">
        <DataGrid ItemsSource="{Binding SportRatings}">
            
        </DataGrid>
    </GroupBox>
</UserControl>
